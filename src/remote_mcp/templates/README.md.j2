# {{ service_name }} MCP

Remote MCP (Model Context Protocol) server for {{ service_name }}, scaffolded with
[`remote-mcp`](https://github.com/pushpendra-tripathi/remote-mcp).

## Deploy

[![Deploy to Render](https://render.com/images/deploy-to-render-button.svg)](https://render.com/deploy)
[![Deploy on Railway](https://railway.app/button.svg)](https://railway.app/new/template)
[![Deploy on Fly.io](https://fly.io/static/images/launch/launch.svg)](https://fly.io/launch)

> Replace the link targets with your repo URL (e.g.
> `https://render.com/deploy?repo=https://github.com/<you>/{{ project_name }}`)
> after pushing this scaffold to GitHub. `render.yaml` and `fly.toml` are
> already configured.

## Quick start

```bash
python -m venv venv && source venv/bin/activate
pip install -e ".[dev]"
cp env.example .env
uvicorn asgi:application --reload --port 8001 --lifespan on
```

Server: http://localhost:8001
MCP endpoint: http://localhost:8001/mcp
OAuth discovery: http://localhost:8001/.well-known/oauth-authorization-server

## Layout

```
src/
├── server.py              # FastMCP instance + tool mounts
├── app.py                 # Starlette factory
├── config/settings.py     # Pydantic settings (env-driven)
├── core/                  # auth, errors, http_client, telemetry, handlers
├── middleware/            # auth + telemetry middleware
├── views/                 # health, oauth discovery, landing
└── tools/                 # your tools live here
```

## Adding a tool

```bash
remote-mcp add tool my_tool
```

Then mount in `src/server.py`:

```python
from src.tools.my_tool import my_tool_router
mcp.mount(my_tool_router)
```

## Security model

**API tools act as the caller; database tools act as the server.** API-backed
tools forward the caller's Bearer token, so your backend enforces the
caller's permissions. Database tools (added via `remote-mcp add database`)
run with the server's own `DATABASE_URL` credentials — anything the MCP
layer admits can read every allowlisted table. Keep the allowlist tight and
leave writes opt-in.

## Tests

```bash
pytest
```

## Deployment

See [DEPLOYMENT.md](DEPLOYMENT.md) for environment variables, Docker, and
Render/Railway/Fly.io notes.

## License

MIT.

## Publish to the MCP Registry

This project ships registry-ready metadata:

1. Edit `server.json`: replace `YOUR-DOMAIN` with your deployed domain{% if github_owner == "YOUR-GITHUB-USERNAME" %} and `YOUR-GITHUB-USERNAME` with your GitHub user/org{% endif %}.
2. Make sure the server is publicly reachable at the `remotes[].url`.
3. Push a version tag: `git tag v1.0.0 && git push --tags`.

`.github/workflows/publish-mcp.yml` authenticates via GitHub OIDC and publishes
to the official registry (registry.modelcontextprotocol.io), which feeds the
marketplaces MCP clients use for discovery.
